import style from "./Nav.module.css";
const { container, mainTitle, secondText, startButton, imgBack } = style;

const Nav = () => {
    return (
        <div>
            <h2 className={style.container}>Nav</h2>
        </div>
    )
}

export default Nav
